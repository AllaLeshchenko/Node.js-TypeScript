Метод POST в HTTP используется для отправки данных на сервер, и часто именно для создания или обработки действий, которые изменяют данные.

Он:

не ограничен идемпотентностью (можно вызывать сколько угодно раз — и результат может отличаться)

может менять данные на сервере

часто используется для логики "действий" — например: регистрация, логин, смена пароля, отправка формы и т.д.

🛠 Примеры использования POST в реальных API
Маршрут	        Метод	Что делает
/login	        POST	Выполняет вход
/register	POST	Регистрирует нового пользователя
/change-passwordPOST	Меняет пароль
/reset-password	POST	Запрос на сброс пароля
/send-email	POST	Отправляет email

➡️ То есть POST — это "выполни действие", в отличие от PUT, который говорит "обнови объект полностью".

🔄 Альтернатива: PUT или PATCH
PUT /user/password — логично, но требует полного обновления ресурса, обычно всего пользователя (что избыточно).

PATCH /user/password — более "REST-стильно", если ты обновляешь только одно поле.

Но в практике, особенно когда речь идет о действиях вроде "смена пароля", почти все используют POST, потому что:

Это не просто "обновление ресурса", а "выполнение защищённого действия с проверкой".

Так какой метод правильный?
Ситуация	                Метод	Почему
Простое обновление имени	PATCH	Обновляем одно поле
Обновление всей учётной записи	PUT	Полностью заменяем объект
Смена пароля с проверкой	POST ✅	Это действие, не просто update
Отправка формы на регистрацию	POST	Передаём данные на сервер
